# FE 공유 — 동일 파일명 업로드 흐름 정리 (2026-08-07)

> 요약
> 1. **확인창을 하나로 합친다** — "대체할까?" 묻고 다시 "이름 뭘로?" 묻는 2단 모달을 없앤다
> 2. **`별도 문서로 등록` 선택 시 추천 파일명을 미리 채워 준다** (수정 가능)
> 3. **업로드 시 `expect_version`을 함께 보낸다** (신규 파라미터) → 그 사이 DB가 바뀌었으면 **409**
> 4. `exists` 조회는 **파일 선택 직후 한 번만**. 추천 파일명은 이미 받아둔 문서 목록으로 로컬 계산

DB 스키마 변경 없음. 로컬 환경 재구축 불필요.

---

## 1. 왜 바뀌나

지금은 같은 이름이 있을 때 모달이 두 번 뜬다 — ① 대체할지 묻고 ② 아니라고 하면 이름을 묻는다.
①에서 이미 의사를 밝혔는데 한 번 더 멈추는 게 번거롭다는 지적이 있었다.

**자동으로 `(1)`을 붙여 물어보지 않고 올리는 방식은 채택하지 않았다.** 이 서비스에서 파일명은
단순 라벨이 아니라 문서의 식별자이고, 검색 임베딩 입력과 답변의 출처 표기에 그대로 쓰인다.
`A문서 (1).pdf` 같은 정보 없는 이름이 싸게 만들어지면 상담원이 보는 인용이
`[A문서 (1).pdf v1]`이 되고, 이름만으로 구분이 안 되는 문서 쌍이 KB에 쌓인다.
**추천값은 채워 주되 고칠 기회는 남긴다**가 이번 결론이다.

---

## 2. 화면 흐름

```
파일 선택
   ↓
GET /kms/documents/exists?filename=A문서.pdf        ← 조회는 이 한 번뿐
   ↓
exists=false ──────────────────→ 바로 업로드 (expect_version=0)
exists=true
   ↓
┌───────────────────────────────────────────────────┐
│ 'A문서.pdf' 문서가 이미 등록되어 있습니다.          │
│ (현재 v1, 2026-08-05 등록)                        │
│                                                   │
│  ○ 기존 문서를 대체                                │
│     이전 버전은 검색에서 제외되며 되돌릴 수 없습니다 │
│                                                   │
│  ○ 별도 문서로 등록                                │
│     [ A문서 (1).pdf               ]  ← 수정 가능   │
│                                                   │
│                            [취소]  [업로드]        │
└───────────────────────────────────────────────────┘
   ↓
POST /kms/documents
   대체      → filename=A문서.pdf,     expect_version=1
   별도 문서 → filename=A문서 (1).pdf, expect_version=0
   ↓
200 → 응답의 filename을 토스트로 노출
409 → 확인창 재표시 (본문의 filename·current_version 반영)
```

### `exists`를 두 번 부르지 않는다

이름을 `A문서 (1).pdf`로 고쳤으니 그 이름도 확인해야 할 것 같지만, **`exists`를 다시 부르지 않는다.**
조회와 업로드 사이에는 어차피 틈이 남아 사전 조회로는 못 막고, 아래 409가 같은 일을 더 정확하게 한다.
왕복도 하나 준다.

### 추천 파일명은 로컬에서 계산한다

문서 목록 화면은 이미 `GET /kms/documents`로 테넌트 전체 문서를 들고 있다. 거기서 빈 번호를 찾으면
추가 호출이 없다. 규칙:

```
base = 업로드한 파일명의 확장자 제외 부분    (그대로 사용 — 번호를 파싱하지 않는다)
후보 = "base (i)" + 확장자,  i = 1, 2, 3, ...    (윈도우 탐색기 관례대로 공백 한 칸)
목록에 있는 이름이면 i를 올린다
```

| 올린 파일 | base | 첫 추천 | 그것도 있으면 |
|---|---|---|---|
| `A문서.pdf` | `A문서` | `A문서 (1).pdf` | `A문서 (2).pdf` |
| `A문서 (3).pdf` | `A문서 (3)` | `A문서 (3) (1).pdf` | `A문서 (3) (2).pdf` |

두 번째 줄이 `A문서 (4).pdf`가 **아닌** 이유: 사용자가 의도해서 `(3)`을 붙인 별개 문서일 수 있다.
번호를 파싱해 같은 가족으로 묶으면 "파일명 완전일치 = 같은 문서"라는 식별 규칙이 둘로 늘어난다.
공백은 관례를 따른 표기일 뿐이고, 서버는 공백까지 구분해 완전일치로 판정하므로 판정에 영향이 없다.

` (1)`은 FE가 붙인 접미사이므로, 그게 실패하면 **버리고 ` (2)`를 붙인다.** `A문서 (1)`을 새 base로
삼지 않는다 (`A문서 (1) (1).pdf`가 되면 안 된다). 단, **사용자가 입력란을 직접 고쳤다면** 그 이름을
새 base로 본다 — `환불정책_최종.pdf`로 고쳐 올렸다가 충돌하면 `환불정책_최종 (1).pdf`.

---

## 3. API 변경 — `POST /kms/documents`

파라미터 하나 추가 (multipart form field). **응답 스키마는 그대로다.**

```
expect_version: int   (선택)
```

| 보내는 값 | 뜻 |
|---|---|
| **미전송** | 검사하지 않음 — 기존 동작 그대로 (조용히 대체됨) |
| **`0`** | "이 이름은 아직 없어야 한다" — 별도 문서로 등록하는 경우 |
| **`N`** | "이 이름의 현재 버전이 N이어야 한다" — 대체하는 경우 |

`exists` 응답의 `version`을 그대로 넘기면 된다. 없는 이름이면 `version`이 `null`로 오니
**`version ?? 0`** 으로 변환한다.

### 409 응답

```jsonc
{
  "detail": "'A문서 (1).pdf' 문서 상태가 변경되었습니다. 다시 확인해 주세요.",
  "filename": "A문서 (1).pdf",   // 충돌한 이름
  "current_version": 1           // 그 이름의 현재 버전 (없으면 0)
}
```

`detail`은 **문자열로 유지**했다 — 공용 에러 토스트가 그대로 쓸 수 있게. 추가 필드는 그 옆에 평평하게 붙는다.

**409를 받으면:**

- **별도 문서 경로** → 그 이름이 방금 선점됐다는 뜻. 후보에서 지우고 다음 번호로 확인창 재표시.
  문구에 `filename`을 넣어 어느 이름이 걸렸는지 밝혀야 한다 — 안 그러면 같은 창이 두 번 떠서 버그처럼 보인다.
  > "`A문서 (1).pdf`도 이미 등록되어 있습니다. (현재 v1)" + 추천값 `A문서 (2).pdf`
- **대체 경로** → 그 사이 남이 새 버전을 올렸다는 뜻.
  > "화면에는 v1이었지만 지금은 v`current_version`입니다. 그래도 대체하시겠습니까?"
  >
  > 사용자가 계속하겠다면 `expect_version`을 `current_version`으로 바꿔 재전송한다.

무한 루프는 없다 — 별도 문서 경로는 번호가 매번 전진하고, 대체 경로는 사용자 확인을 거친다.

### 왜 서버 검사가 필요한가

조회하고 확인창을 띄우는 구조는 조회~업로드 사이에 틈이 남는다. 화면에서 몇 번을 확인하든
그 틈에 다른 사람이 같은 이름을 올리면 **남의 문서가 조용히 대체된다** — 구버전은 검색에서 빠지고
청크가 삭제되며 되돌릴 수 없다. 빈도는 낮지만(같은 초에 같은 이름) 사고의 성질이 나쁘다.
`expect_version`은 그 창을 서버 트랜잭션 안에서 닫는다.

### 부수 개선

같은 파일을 동시에 두 번 올리면 지금까지 **500**이 났다. 이제 같은 **409**로 온다.
`expect_version`을 안 보내는 호출도 마찬가지다.

---

## 4. 구현 상태 — FE·BE 모두 반영 완료 (2026-08-07)

**백엔드** (`consult-agent`)

- `routers/documents.py` — `expect_version` 파라미터, 409 응답(`_version_conflict`), `IntegrityError` → 409 매핑, 409 시 blob 정리
- `tests/test_integration_documents.py` — 낙관적 잠금 9건 추가 (전체 227 통과)

**FE** (`isac-cc-application/frontend/src/pages/KMS`)

- `api.ts` — `uploadDocument(file, description?, expectVersion?)`, `UploadConflictError`
- `Documents/index.tsx`
  - `DuplicateDialog` — 파일별 라디오(대체 / 별도 문서로) + 수정 가능한 이름 입력란. 2단 `window.confirm` 제거
  - `nextFreeName` — 번호 파싱 없이 ` (1)`, ` (2)`… 문서 목록에서 로컬 계산 (`findFreeName`의 `exists` 반복 조회 제거)
  - `askConflict` — 409 복구. 별도 문서 경로는 다음 번호 제안, 대체 경로는 "그래도 대체?" 재확인. 파일당 최대 5회
  - 중복이 아닌 파일도 `expect_version=0`으로 보내 조용한 대체를 막는다

기존 이름 규칙이 `'환불정책 (2).pdf'`(공백 + 2부터, `(n)` 파싱)에서
**`'환불정책 (1).pdf'`(1부터 시작, `(n)` 파싱 없음)** 으로 바뀌었다. 공백 한 칸은 그대로 유지.

---

## 5. 참고 — 검토했지만 접은 안

**서버가 파일명을 자동 확정 (`on_conflict=new_document` → 서버가 빈 번호를 찾아 `(1)` 부여)**

경쟁도 확실히 막고 클릭도 가장 적지만 접었다.

- 얻는 건 프롬프트 한 번인데, 빈 번호 탐색 + 유니크 위반 재시도 + blob 정리 + 폴더 계승 규칙이 붙는다
- 새 이름이라 폴더를 계승할 대상이 없어 **미분류로 떨어진다.** 폴더의 참조 설명이 검색 순위 계산에
  쓰이므로, 사실상 같은 내용인데 형제 문서와 순위가 갈린다
- 무엇보다 **의미 없는 이름을 만드는 쪽이 가장 쉬운 길이 된다.** 이름이 검색 품질과 출처 표기에
  직접 쓰이는 구조에서는 그 마찰이 값을 한다

이름 확정을 FE에 남기고 경쟁만 `expect_version`으로 막는 지금 안이, 위 문제 전부를 함께 없앤다.
