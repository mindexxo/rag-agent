# FE 공유 — 변경 요약 (2026-08-07)

---

## 1. 폴더 — '참조 설명' 추가

폴더에 **"이 폴더의 지식을 언제 참조해야 하는지"** 를 자연어로 적는 칸이 생겼습니다. 선택 입력, 200자.

검색 순위 계산(리랭커)에 들어가므로 잘 적으면 그 폴더 문서가 관련 질문에서 위로 올라옵니다.
반대로 **틀리거나 뭉뚱그린 설명("기타", "각종 문서")은 검색을 나쁘게 만듭니다.** 짧고 구체적으로.

## 2. 문서 업로드 — 동일 파일명 처리 개편

**같은 파일명 = 같은 문서**로 판단해 재업로드하면 기존 문서를 대체합니다(옛 버전은 검색에서 빠지고 청크 삭제, 되돌릴 수 없음). 그래서 파일을 보내기 **전에** 확인합니다.

- 파일 선택 직후 서버에 같은 이름이 있는지 조회 → 있으면 확인창
- 추천 이름은 `환불정책 (1).pdf` 형식. 이미 있으면 `(2)`, `(3)`… **수정 가능**합니다
- 기본 선택은 '별도 문서로 등록' — 실수로 대체하는 것보다 파일이 하나 더 생기는 편이 복구하기 쉬움
- 여러 개를 한 번에 올리면 확인창도 **배치당 한 번**만 뜨고, 파일별로 각각 선택합니다

**이름을 자동으로 확정해 버리지는 않습니다.** 파일명이 단순 라벨이 아니라 문서의 식별자이고, 검색 임베딩 입력과 답변의 출처 표기(`[환불정책 (1).pdf v1]`)에 그대로 쓰이기 때문입니다. 의미 없는 이름이 쌓이면 검색 품질이 조용히 나빠져서, 추천값은 채워 주되 고칠 기회를 남겼습니다.

**동시 업로드 사고 방지 (서버 검사 추가)**

조회하고 확인창을 띄우는 방식은 조회~업로드 사이에 틈이 남습니다. 그 틈에 다른 사람이 같은 이름을 올리면 **남의 문서가 조용히 대체됩니다.** 화면에서 몇 번을 확인해도 이 틈은 못 막습니다.

그래서 업로드 시 "내가 본 상태"를 함께 보내고, 서버가 트랜잭션 안에서 검증합니다. 다르면 409로 거절하고 FE가 다시 물어봅니다(별도 문서 경로는 다음 번호 제안, 대체 경로는 "그 사이 v2가 됐다, 그래도 대체?").

부수 효과로, 같은 파일을 동시에 두 번 올렸을 때 나던 **500이 409로** 바뀌었습니다.

## 3. 채팅 — 인용 표시 두 건 수정

- **각주 오노출**: 스트리밍 시작 시점에는 아직 인용 라벨이 안 나와서 "라벨 없는 답변"으로 오판해 **검색 후보 전부가 각주로 떴다가 줄어들었습니다.** 답변에 쓰이지도 않은 문서를 근거로 오인하게 만드는 문제라 수신 중에는 폴백을 끕니다
- **깜빡임**: 아직 닫히지 않은 인용 라벨 조각(`...입니다 [환불정책`)이 화면에 노출됐다가 `]`가 도착하면 사라지던 현상 수정

## 4. 운영 리포트 — 항목 정리

"운영자가 보고 행동할 수 있는 것만" 남기는 방향으로 정리했습니다. `GET /kms/stats` 응답이 바뀝니다.

**제거** (응답 필드 자체가 사라짐 — FE 반영 완료)

- `active_users` — 인증 전엔 X-User-Id가 하드코딩이라 항상 1명. 거짓 숫자 노출 방지
- `avg_latency_ms` · `p95_latency_ms` — 응답 속도는 우리 내부 SLO 지표
- `blocked` · `failed` — 장애·정책 신호라 리포트가 아니라 알람 소관

**변경**

- 답변률 분모가 **지식 질문만**으로 좁아짐 — 인사·요약 같은 잡담 답변은 거절이 나올 수 없어서, 분모에 넣으면 답변률이 부풀었음. 신규 필드 `knowledge_done`(분모)이 추가되고 `refusal_rate`는 이 기준으로 계산됨
- `daily` — 질문 없는 날도 0으로 채워서 옴 (기간 내 연속, 구멍 없음)

**유지**: 질문 수, 일별 추이, 인용 상위 문서, 미답변 질문 목록

## 5. 로컬 환경 — 재구축 필요 (스키마 변경 있음)

**최신 `kms-db.tar.gz`를 받아 로컬 DB를 다시 만들어야 합니다.**

| 변경                      | 내용                                 |
| ------------------------- | ------------------------------------ |
| `documents.sha256`        | 삭제                                 |
| `documents.version_label` | 삭제                                 |
| `folders.description`     | 추가 (폴더 참조 설명)                |
| `messages.intent`         | 추가 (답변률 분모 — 리포트 정리 §4)  |

```bash
docker compose -f docker-compose.local.yml down -v     # -v = 볼륨(pgdata)까지 삭제
docker load -i kms-db.tar.gz
docker compose -f docker-compose.local.yml up -d
```

**`-v`가 없으면 스키마가 안 바뀝니다.** `initdb.d`는 데이터 디렉터리가 비어 있을 때 최초 1회만 실행되므로, 이미지를 새로 받아도 볼륨이 남아 있으면 옛 스키마 그대로입니다.

로컬에 올려둔 문서·대화 기록은 전부 사라집니다(테스트 데이터뿐이면 문제없음). `folders.description`이 빠지면 **검색 요청이 전부 실패**합니다.

**이미 앞선 이미지(8/7 오전)로 재구축을 마친 경우** — 전체 재구축 대신 psql 한 줄이면 됩니다 (데이터 유지):

```sql
ALTER TABLE messages ADD COLUMN IF NOT EXISTS intent TEXT;
```

⚠ **순서 주의: 백엔드 코드를 받기 전에 DB부터 반영하세요.** 위 ALTER는 구코드와 호환이라
먼저 실행해도 무해하지만, 반대로 코드만 받고 DB를 반영하지 않으면 **채팅·대화 이력을 포함한
messages 관련 기능이 전부 실패**합니다 (리포트만이 아님).