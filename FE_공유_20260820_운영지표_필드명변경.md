# FE 공유 — 운영 리포트 지표 필드명 변경 (2026-08-20)

> 요약
> 1. `GET /kms/stats` 응답의 **`refusals` → `ungrounded`**, **`refusal_rate` → `ungrounded_rate`** 로 이름이 바뀐다
> 2. 화면 라벨도 "거절률"에서 **"근거 미확인율"** 쪽으로 바꾸는 게 맞다 (의미가 넓어졌다)
> 3. **DB 재생성 불필요** — 서버 내부 컬럼만 정리했고 FE가 쓰는 스키마는 그대로다

---

## 1. 무엇이 바뀌나

`GET /kms/stats` 응답에서 두 필드의 **이름만** 바뀐다. 계산 위치·분모·응답 구조는 그대로다.

```jsonc
// 이전
{
  "period_days": 7,
  "questions": 128,
  "knowledge_done": 96,
  "refusals": 12,          // ← 바뀜
  "refusal_rate": 0.125,   // ← 바뀜
  "daily": [...],
  "top_documents": [...]
}

// 이후
{
  "period_days": 7,
  "questions": 128,
  "knowledge_done": 96,
  "ungrounded": 12,
  "ungrounded_rate": 0.125,
  "daily": [...],
  "top_documents": [...]
}
```

`GET /kms/stats/unanswered`는 **응답 형태가 안 바뀐다**(`question`, `asked_at` 그대로). 다만 목록에 담기는 대상의 기준이 바뀌었다 — 아래 3절 참조.

## 2. 왜 바뀌나

지금까지 "거절"을 **답변 문구로** 판정했다. 답변에 `"제공된 문서에서 확인할 수 없"` 이라는 문구가 들어 있으면 거절로 셌다.

이 방식에 두 문제가 있었다.

**하나 — 문구가 조금 달라지면 못 잡는다.** 실제로 이런 답변이 거절로 안 세어졌다:

```
"해외 배송은 제공되지 않습니다."       ← 근거를 못 찾았는데 문구가 안 맞아 정상 답변으로 집계
```

**둘 — 프롬프트를 손댈 수 없다.** 답변 지침 문구를 조금만 바꾸면 검출률이 95% → 3%까지 떨어진다(실측). 지표가 프롬프트에 묶여 있어서 품질 개선을 막고 있었다.

그래서 판정을 **"답변이 인용한 문서가 몇 건인가"** 로 바꿨다. 0건이면 근거를 못 댄 것이다. 이건 문구와 무관하고, 이미 화면 각주에 쓰고 있는 값과 같은 데이터다.

## 3. 의미가 조금 넓어졌다 — 라벨을 바꾸는 게 좋다

새 기준은 옛 "거절"을 포함하면서 조금 더 넓다.

| 답변 | 옛 기준(거절) | 새 기준(근거 미확인) |
|---|---|---|
| "제공된 문서에서 확인할 수 없습니다" | 포함 | 포함 |
| "해외 배송은 제공되지 않습니다" (근거 없음) | **누락** | 포함 |
| 근거 없이 그럴듯하게 답한 경우 | **누락** | 포함 |
| 문서를 인용해 답한 경우 | 제외 | 제외 |

즉 **"답을 거부한 비율"이 아니라 "근거를 대지 못한 비율"** 이다. 세 번째 줄(근거 없이 답함)이 새로 포함되는데, 운영자 입장에서는 그게 오히려 봐야 하는 신호다.

화면 문구 제안:
- `ungrounded_rate` → **"근거 미확인율"** 또는 **"미근거 답변 비율"**
- `/kms/stats/unanswered` 목록 → **"지식 갭"** 은 그대로 쓸 수 있다 (목적이 KB 보강이라 동일)

다만 목록에 "실제로는 답변이 달렸지만 근거가 없던 것"이 섞일 수 있다는 점은 알고 있어야 한다. 응답만으로는 그 둘을 구별할 수 없다.

## 4. 수치가 얼마나 움직이나

**급격한 변동은 없을 것으로 본다.** 골드셋 450문항 대조에서 두 판정의 불일치는 0건이었다. 다만 위 표의 2·3번 유형이 새로 잡히므로 **소폭 상승**할 수 있다 — 그건 지표가 정확해진 것이고 품질이 나빠진 것이 아니다.

운영 배포 전이라 실사용 추이가 아직 없다. 즉 이 전환으로 **과거 그래프가 어긋나는 문제는 없다.**

## 5. DB 재생성 — 불필요

서버 쪽에서 `messages.is_refusal` 컬럼을 제거했지만, 이건 **서버만 쓰던 내부 컬럼**이다. FE가 조회하는 API 응답에는 원래 나가지 않았다.

로컬 DB 이미지를 그대로 쓰면 된다 — 컬럼이 남아 있어도 서버가 참조하지 않으므로 동작에 지장이 없다.

## 6. FE 체크리스트

- [ ] `refusal_rate`를 읽는 곳을 `ungrounded_rate`로 교체
- [ ] `refusals`를 읽는 곳을 `ungrounded`로 교체
- [ ] 화면 라벨 "거절률" → "근거 미확인율" (3절 참조)
- [ ] `/kms/stats/unanswered` 는 코드 변경 없음 (툴팁·설명 문구만 검토)
